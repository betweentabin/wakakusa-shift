2. 発注管理機能の概要

スライド資料「発注管理機能」[20-31]によると、本機能は以下の要素を含むことが示唆されています。

•
在庫修正: 在庫数の調整機能。

•
在庫: 現在の在庫状況の表示。

•
URL: 発注先のURLの管理。

•
AA: 項目名が不明ですが、スライドの画像から品目コードや品目名のようなものと推測されます。

•
30: 在庫数または発注量の例示と推測されます。

これらの情報から、発注管理機能は、水耕栽培に必要な資材（種子、肥料、培地など）の在庫を管理し、適切なタイミングで発注を行うためのシステムであることが理解できます。特に、「在庫数、発注先URL,入庫して数量を増やせるなど」という記述[1]から、在庫の増減管理と発注プロセスのサポートが主要な機能要件となります。

3. 機能要件詳細

発注管理機能の具体的な要件を以下に詳述します。

3.1. 品目管理

•
品目登録: 発注対象となる資材の品目名、品目コード、単位、初期在庫数などを登録できること。

•
品目情報編集: 登録済みの品目情報を編集できること。

•
発注先URL登録: 各品目に対応する発注先のURLを登録・管理できること。これにより、発注時にスムーズに発注サイトへアクセスできる。

3.2. 在庫管理

•
在庫数表示: 各品目の現在の在庫数をリアルタイムで表示できること。

•
在庫修正機能: 誤入力や物理的な在庫差異が発生した場合に、手動で在庫数を修正できること。修正履歴を記録すること。

•
入庫処理: 発注した資材が納品された際に、入庫処理を行い在庫数を増加できること。入庫日、入庫数量、担当者などの情報を記録できること。

•
出庫処理: 資材が使用された際に、出庫処理を行い在庫数を減少できること。出庫日、出庫数量、使用目的、担当者などの情報を記録できること。

•
閾値設定とアラート: 各品目に対して、在庫が一定数を下回った場合にアラートを出すための閾値を設定できること。アラートはシステム通知、またはメール通知で行うこと。

3.3. 発注処理

•
発注リスト作成: 在庫閾値を下回った品目や、手動で選択した品目を発注リストに追加できること。

•
発注量入力: 各品目の発注量を入力できること。

•
発注先選択: 登録されている発注先URLから、発注先を選択できること。

•
発注履歴: 発注日時、発注品目、発注量、発注先、発注ステータス（発注済み、納品済みなど）を記録し、参照できること。

4. システム設計

4.1. アーキテクチャ

わかくさ福祉会様の費用面での制約を考慮し、無料サービスやツールを優先的に利用する方針に基づき、以下のアーキテクチャを提案します。

•
フロントエンド: Webベースのアプリケーションとして開発し、PCやタブレットからアクセス可能とする。ReactなどのモダンなJavaScriptフレームワークを利用し、ユーザーフレンドリーなUI/UXを実現する。

•
バックエンド: Python (Flask) を利用したAPIサーバーを構築する。データ処理、ビジネスロジック、データベースとの連携を担当する。

•
データベース: PostgreSQLまたはSQLiteなどのオープンソースデータベースを利用する。特に、小規模な運用であればSQLiteは設定が容易で運用コストも低い。

•
デプロイメント: HerokuやGoogle Cloud Run、AWS Fargateなどの無料枠があるPaaS/FaaSを利用し、インフラコストを最小限に抑える。

4.2. データベーススキーマ（案）

items テーブル (品目情報)

カラム名データ型制約説明item_idINTEGERPRIMARY KEY品目IDitem_codeTEXTUNIQUE品目コード（例: AA）item_nameTEXTNOT NULL品目名unitTEXT
単位（例: 個、袋）order_urlTEXT
発注先URLthresholdINTEGERDEFAULT 0在庫閾値

inventory テーブル (在庫情報)

| カラム名       | データ型   | 制約       | 説明                                   |
| :------------- | :--------- | :--------- | :------------------------------------- |\n| inventory_id | INTEGER    | PRIMARY KEY | 在庫ID                                 |
| item_id      | INTEGER    | NOT NULL   | 品目ID (FK: items.item_id)           |
| current_stock| INTEGER    | NOT NULL   | 現在の在庫数                           |
| last_updated | DATETIME   | NOT NULL   | 最終更新日時                           |

transactions テーブル (入出庫履歴)

カラム名データ型制約説明transaction_idINTEGERPRIMARY KEYトランザクションIDitem_idINTEGERNOT NULL品目ID (FK: items.item_id)transaction_typeTEXTNOT NULLトランザクションタイプ (入庫/出庫/修正)quantityINTEGERNOT NULL数量transaction_dateDATETIMENOT NULLトランザクション日時user_idINTEGER
担当者ID (FK: users.user_id)notesTEXT
備考

orders テーブル (発注履歴)

カラム名データ型制約説明order_idINTEGERPRIMARY KEY発注IDitem_idINTEGERNOT NULL品目ID (FK: items.item_id)ordered_quantityINTEGERNOT NULL発注数量order_dateDATETIMENOT NULL発注日時supplier_urlTEXT
発注先URLstatusTEXTNOT NULLステータス (発注済み/納品済み/キャンセル)user_idINTEGER
発注担当者ID (FK: users.user_id)

5. 開発フェーズ

以下のフェーズで開発を進めます。

フェーズ1: 要件定義と設計（完了）

•
スライド資料の分析と機能要件の明確化。

•
システムアーキテクチャおよびデータベーススキーマの設計。

フェーズ2: バックエンド開発

•
API実装: 品目管理、在庫管理、入出庫処理、発注処理に関するRESTful APIをFlaskで実装。

•
データベース連携: 設計したスキーマに基づき、データベースとの連携を実装。

•
単体テスト: 各APIエンドポイントの単体テストを実施。

フェーズ3: フロントエンド開発

•
UIコンポーネント開発: 品目一覧、在庫状況表示、入出庫フォーム、発注フォームなどのUIコンポーネントをReactで開発。

•
API連携: バックエンドAPIとの連携を実装。

•
画面フロー実装: ユーザーがスムーズに操作できる画面遷移とインタラクションを実装。

フェーズ4: 結合テストとデプロイ

•
結合テスト: フロントエンドとバックエンドを結合し、エンドツーエンドのテストを実施。

•
デプロイ: 開発環境から本番環境へのデプロイ。

•
ユーザー受け入れテスト (UAT): わかくさ福祉会様による機能確認とフィードバックの収集。

フェーズ5: 運用・保守

•
システム監視、バグ修正、機能改善、セキュリティアップデートなど。

6. 今後の展望

発注管理機能の基本機能が確立された後、以下の機能拡張を検討できます。

•
自動発注機能: 在庫閾値を下回った品目に対して、自動的に発注処理を行う機能。

•
発注承認ワークフロー: 発注前に上長の承認を必要とするワークフロー機能。

•
レポート機能: 在庫推移、発注傾向などのレポートを生成する機能。

•
サプライヤー連携: 主要サプライヤーのシステムと連携し、在庫情報や価格情報を自動で取得する機能。

7. 参照

[1] わかくさ福祉会様_水耕栽培_管理システム案(1).pptx, スライド1
[2] わかくさ福祉会様_水耕栽培_管理システム案(1).pptx, スライド20-31





著者: Manus AI
日付: 2025年8月8日

3.4. ユーザーと権限管理

発注管理機能を利用するユーザーの役割と権限を明確にする必要があります。

•
管理者: 全ての品目情報、在庫情報、発注履歴の閲覧・編集、ユーザー管理、システム設定が可能。

•
一般ユーザー: 担当する品目の在庫情報、発注履歴の閲覧、入出庫処理、発注処理が可能。品目情報の編集やシステム設定は不可。

3.5. 監査ログ

システムの透明性とトレーサビリティを確保するため、以下の操作について監査ログを記録します。

•
品目情報の登録、編集、削除

•
在庫数の修正、入庫、出庫

•
発注の作成、更新、キャンセル

•
ユーザーのログイン、ログアウト、権限変更

ログには、操作日時、操作ユーザー、操作内容、変更前後のデータを含めること。

4.3. 技術スタックの選定理由

4.3.1. フロントエンド (React)

Reactは、宣言的なUI構築、コンポーネントベースの設計、豊富なコミュニティサポートにより、効率的かつ保守性の高いフロントエンド開発を可能にします。特に、SPA (Single Page Application) としての動作により、ユーザー体験の向上とバックエンドへの負荷軽減が期待できます。また、無料のホスティングサービス（例: Netlify, Vercel）が利用可能であり、運用コストを抑えることができます。

4.3.2. バックエンド (Python/Flask)

Pythonは、そのシンプルさと豊富なライブラリエコシステムにより、迅速な開発と高い生産性を実現します。Flaskは軽量なWebフレームワークであり、小規模から中規模のAPIサーバー構築に適しています。必要な機能のみを組み込むことで、リソース消費を抑え、無料枠での運用を容易にします。また、Pythonはデータ処理や分析にも強みを持つため、将来的な機能拡張（例: 発注予測、在庫最適化）にも対応しやすいです。

4.3.3. データベース (PostgreSQL/SQLite)

PostgreSQLは、堅牢性、拡張性、豊富な機能を持つオープンソースのリレーショナルデータベースです。将来的なデータ量の増加や複雑なクエリにも対応可能です。SQLiteは、サーバーレスでファイルベースのデータベースであり、初期開発や小規模な運用において非常に手軽に利用できます。デプロイメントの容易さと運用コストの低さが魅力です。無料枠のあるPaaSではPostgreSQLが提供されることが多いため、開発初期はSQLite、デプロイ時はPostgreSQLへの移行を検討します。

4.4. API設計（例）

4.4.1. 品目管理API

•
GET /api/items: 全品目リストの取得

•
GET /api/items/{item_id}: 特定品目の詳細取得

•
POST /api/items: 新規品目の登録

•
PUT /api/items/{item_id}: 特定品目の情報更新

•
DELETE /api/items/{item_id}: 特定品目の削除

4.4.2. 在庫管理API

•
GET /api/inventory: 全品目の在庫状況取得

•
GET /api/inventory/{item_id}: 特定品目の在庫状況取得

•
POST /api/inventory/inbound: 入庫処理

•
リクエストボディ: {'item_id': int, 'quantity': int, 'user_id': int, 'notes': str}



•
POST /api/inventory/outbound: 出庫処理

•
リクエストボディ: {'item_id': int, 'quantity': int, 'user_id': int, 'purpose': str, 'notes': str}



•
PUT /api/inventory/{item_id}/adjust: 在庫修正

•
リクエストボディ: {'new_stock': int, 'user_id': int, 'reason': str}



4.4.3. 発注管理API

•
GET /api/orders: 全発注履歴の取得

•
GET /api/orders/{order_id}: 特定発注の詳細取得

•
POST /api/orders: 新規発注の作成

•
リクエストボディ: {'item_id': int, 'ordered_quantity': int, 'supplier_url': str, 'user_id': int}



•
PUT /api/orders/{order_id}/status: 発注ステータスの更新

•
リクエストボディ: {'status': str, 'user_id': int}



5. 開発フェーズ詳細

フェーズ2: バックエンド開発

•
期間: 4週間

•
担当: 開発チーム

•
成果物:

•
RESTful APIドキュメント

•
単体テストコード

•
データベースマイグレーションスクリプト



•
詳細タスク:

1.
開発環境構築 (Python, Flask, データベース)

2.
データベーススキーマの定義とマイグレーションツール導入

3.
品目管理APIの実装とテスト

4.
在庫管理APIの実装とテスト (入庫、出庫、修正)

5.
発注管理APIの実装とテスト

6.
ユーザー認証・認可機能の検討と実装 (初期段階では簡易的なもの)

7.
監査ログ機能の実装



フェーズ3: フロントエンド開発

•
期間: 5週間

•
担当: 開発チーム

•
成果物:

•
Webアプリケーション (React)

•
UI/UXデザインドキュメント

•
結合テスト計画



•
詳細タスク:

1.
デザインシステムの選定と基本コンポーネントの作成

2.
品目一覧画面の開発

3.
在庫状況表示画面の開発

4.
入出庫フォーム画面の開発

5.
発注フォーム画面の開発

6.
発注履歴画面の開発

7.
ユーザー認証・認可フローのUI実装

8.
レスポンシブデザインの対応



フェーズ4: 結合テストとデプロイ

•
期間: 3週間

•
担当: 開発チーム、わかくさ福祉会様

•
成果物:

•
テスト報告書

•
デプロイ手順書

•
ユーザーマニュアル（簡易版）



•
詳細タスク:

1.
開発環境での結合テスト実施

2.
テストデータ投入

3.
本番環境へのデプロイ準備 (PaaS選定、設定)

4.
本番環境へのデプロイ

5.
ユーザー受け入れテスト (UAT) の実施とフィードバック収集

6.
UATで発見された課題の修正



フェーズ5: 運用・保守

•
期間: 継続的

•
担当: 開発チーム、わかくさ福祉会様

•
成果物:

•
システム監視レポート

•
改善提案書



•
詳細タスク:

1.
システム稼働状況の監視 (ログ監視、パフォーマンス監視)

2.
定期的なバックアップの実施

3.
バグ修正とパッチ適用

4.
機能改善要望のヒアリングと優先順位付け

5.
セキュリティ脆弱性への対応



6. 今後の展望（詳細）

6.1. 自動発注機能

在庫閾値を下回った品目に対し、自動的に発注処理を行う機能です。これにより、手動での発注作業を削減し、在庫切れのリスクを低減します。

•
機能詳細:

•
設定された閾値を下回った品目を自動検知。

•
事前に登録された発注先URLと発注量に基づき、自動で発注リストを作成。

•
管理者の承認後、または設定により自動で発注処理を実行。



•
技術的考慮事項:

•
外部ECサイトとのAPI連携（可能な場合）。

•
自動発注の頻度とタイミングの設定。

•
発注失敗時の通知メカニズム。



6.2. 発注承認ワークフロー

発注の承認プロセスをシステム化し、誤発注や不正発注を防止します。特に高額な資材や大量発注の場合に有効です。

•
機能詳細:

•
発注申請後、指定された承認者（例: 上長）に通知。

•
承認者はシステム上で発注内容を確認し、承認または却下。

•
承認された発注のみが実行される。



•
技術的考慮事項:

•
承認ルートの柔軟な設定（例: 金額に応じた承認者変更）。

•
承認状況の可視化。

•
承認履歴の記録。



6.3. レポート機能

在庫推移、発注傾向、コスト分析など、経営判断に役立つレポートを生成します。これにより、より効率的な在庫管理とコスト削減に貢献します。

•
機能詳細:

•
期間指定による在庫推移グラフの表示。

•
品目別、発注先別の発注量・金額レポート。

•
入出庫履歴の集計レポート。



•
技術的考慮事項:

•
データ可視化ライブラリの利用（例: Chart.js, D3.js）。

•
CSV/Excel形式でのデータエクスポート機能。

•
定期的なレポート自動生成とメール送信機能。



6.4. サプライヤー連携

主要なサプライヤーのシステムと連携し、リアルタイムで在庫情報や価格情報を取得します。これにより、常に最新の情報を基に発注判断を行うことができます。

•
機能詳細:

•
登録されたサプライヤーのAPIを利用して、品目の在庫数、価格、納期などを自動取得。

•
取得した情報をシステム内の在庫情報と連携表示。



•
技術的考慮事項:

•
各サプライヤーのAPI仕様への対応。

•
API連携のセキュリティ対策。

•
データ同期の頻度とエラーハンドリング。



7. 参照

[1] わかくさ福祉会様_水耕栽培_管理システム案(1).pptx, スライド1
[2] わかくさ福祉会様_水耕栽培_管理システム案(1).pptx, スライド20-31



